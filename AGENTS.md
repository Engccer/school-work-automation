> 🤖 **이 파일은 자동 생성됩니다. 직접 수정하지 마세요.**
> 정본은 `CLAUDE.md` 입니다. 내용을 바꾸려면 `CLAUDE.md` 를 수정한 뒤
> 프로젝트 루트에서 `python sync_agent_docs.py` 를 실행하세요.
> 이 파일을 직접 고치면 다음 동기화 때 경고와 함께 덮어쓰기 대상이 됩니다.

<!-- SYNC-BODY-START: 이 줄 아래 본문은 CLAUDE.md 와 100% 동일하게 자동 생성됨 -->
# school-work-automation 저장소 지침

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고하는 가이드다.

## 저장소 성격 — 공개 반출본

이 저장소는 **공개 배포용 반출본**이다. 정본(원본 스킬)은 저자의 비공개 학교 업무 폴더 `.claude/skills`에 있으며, 저자가 실사용하면서 그쪽을 먼저 갱신한다. 이 저장소는 정본에서 개인·학교 식별 정보를 제거(sanitization)한 뒤 주기적으로 동기화한다.

- 스킬 3종: `neis-automation/`(나이스) · `k-edufine-automation/`(K-에듀파인) · `kyodetong-submit/`(교데통)
- 스킬 `name:`과 폴더명은 ASCII를 쓴다. **한국어 이름은 `npx skills add` 설치 시 슬러그화로 깨져
  `unnamed-skill` 충돌·덮어쓰기가 난다**(실측). 한국어 트리거는 description에 담는다.
- 설치 경로: `npx skills add Engccer/school-work-automation -g`
- 라이선스: MIT

## 반출(정본 → 공개본) 절차

**기준 절차의 정본은 `sanitize-for-release` 스킬**(공개 저장소 `Engccer/sanitize-for-release`)이며, 이 저장소는 그 스킬의 **유형 B(정본/반출본 분리형)** 다. 유형별 절차·스윕 축·오탐 억제는 스킬을 따르고, 여기서는 이 저장소 고유의 것만 적는다.

- **치환 대상**: 교직원·학생 실명, 학교명, 부서명(결재라인 실명 표 포함), 문서번호, 계정 ID, 연락처, 이메일, 개인 로컬 경로, 그리고 **조합하면 특정 학교가 드러나는** 사업명·수치 조합.
- **대체 표기**: `○○중학교장`, `○○담당관`, `기안 → 부장 검토 → 교감 검토 → 교장 결재`처럼 역할·일반형으로. **워크플로우 교훈(시각·소요시간·시행착오)은 보존한다** — 그게 이 공개본의 존재 이유다.
- **구체 패턴은 로컬 전용 `.sanitize.local.md`(gitignored)에 둔다.** 실명 패턴 자체를 이 저장소에 커밋하지 않는다.
- 정본에서 복사할 때 `desktop.ini` 등 드라이브 잡파일은 제외한다.
- 커밋은 의도 파일만 pathspec으로(`git commit -- <경로>`), push까지 완료한다.

## 스킬 갱신 규율

- 각 SKILL.md의 「일반 원칙」에 있는 "외부 공개 전제" 규칙을 유지한다: 갱신 시 개인·학교 식별 정보를 기록하지 않고 일반화해서 쓴다.
- 시행착오·화면 변화는 각 스킬의 변경 이력 섹션에 버전 단위로 누적한다(정본 쪽 관례와 동일).
- 서울 외 시도 사용자 제보(도메인·화면 차이)는 각 스킬의 「적용 범위」 인용 블록에 반영한다.
